# Warehouse Management System API

## Project Overview

**Warehouse Management REST API** - комплексная система управления складом на базе Spring Boot 3.5.7. Система предоставляет полный набор REST API для управления товарами, складами, остатками, перемещениями товаров и информацией о поставщиках.

## 🎯 Назначение проекта

Данная система реализует полнофункциональный REST API для управления складскими операциями:
- Управление каталогом товаров
- Управление несколькими складами
- Отслеживание остатков товаров на каждом складе
- Регистрация и управление перемещениями товаров между складами
- Управление информацией о поставщиках

## 🏗️ Основные сущности

1. **Supplier (Поставщик)** - Информация о поставщиках товаров
   - Имя, контактное лицо, email, телефон, адрес
   - Уникальность: имя и email

2. **Item (Товар)** - Каталог товаров
   - Название, описание, SKU, цена, ID поставщика
   - Уникальность: SKU
   - Ограничения: цена не может быть отрицательной

3. **Warehouse (Склад)** - Физические местоположения складов
   - Название, местоположение, менеджер
   - Уникальность: название

4. **Stock (Остаток)** - Количество товаров на складе
   - Item ID, Warehouse ID, Количество
   - Ограничения: количество не может быть отрицательным
   - Уникальность: комбинация (item_id, warehouse_id)

5. **Movement (Перемещение)** - История передвижения товаров
   - Item ID, Склад-источник, Склад-назначение, Количество
   - Тип движения, Дата, Примечания
   - Ограничения: не может быть перемещения в тот же склад

## 📋 Функциональность

### CRUD Операции

Для каждой сущности реализованы полные CRUD операции:

- **GET** `/api/{entity}` - Получить все элементы
- **GET** `/api/{entity}/{id}` - Получить элемент по ID
- **POST** `/api/{entity}` - Создать новый элемент
- **PUT** `/api/{entity}/{id}` - Обновить элемент
- **DELETE** `/api/{entity}/{id}` - Удалить элемент

### Поддерживаемые точки доступа

- `/api/items` - управление товарами
- `/api/warehouses` - управление складами
- `/api/stocks` - управление остатками
- `/api/movements` - управление перемещениями
- `/api/suppliers` - управление поставщиками

## 🔒 Ограничения и валидация

1. **Валидация остатков**: Количество не может быть отрицательным
2. **Ограничения движения**: Нельзя перемещать товар в тот же склад
3. **Консистентность данных**: При перемещении автоматически обновляются остатки в обоих складах
4. **Уникальность**: SKU товаров, имена поставщиков и email уникальны

## 💾 Технологический стек

- **Backend**: Spring Boot 3.5.7
- **Database**: PostgreSQL
- **ORM**: Spring Data JPA / Hibernate
- **Build**: Gradle
- **Java**: 17+

## 🚀 Запуск приложения

### Предварительные требования

- Java 17 или выше
- PostgreSQL 12+
- Gradle

### Установка и конфигурация

1. Клонируйте репозиторий
2. Скопируйте `.env.example` в `.env` и установите данные для подключения к БД
3. Создайте БД: `warehouse_db`
4. Выполните SQL скрипт: `src/main/resources/schema.sql`

### Запуск

```bash
./gradlew bootRun
```

Приложение запустится на `http://localhost:8080/api`

## 📚 Документация API

Полная документация API с примерами запросов находится в файле [API_DOCUMENTATION.md](API_DOCUMENTATION.md)

## 🔄 Пример использования

### Создание товара
```bash
POST /api/items
Content-Type: application/json

{
  "name": "Ноутбук",
  "description": "Высокопроизводительный ноутбук 15 дюймов",
  "sku": "LAP-001",
  "price": 999.99,
  "supplierId": 1
}
```

### Перемещение товара между складами
```bash
POST /api/movements
Content-Type: application/json

{
  "itemId": 1,
  "fromWarehouse": 1,
  "toWarehouse": 2,
  "quantity": 10,
  "movementType": "TRANSFER",
  "notes": "Перераспределение запасов"
}
```

## 🗄️ Структура БД

- Таблица `supplier` - информация о поставщиках
- Таблица `item` - каталог товаров (внешний ключ на supplier)
- Таблица `warehouse` - информация о складах
- Таблица `stock` - остатки товаров (внешние ключи на item и warehouse)
- Таблица `movement` - история перемещений (внешние ключи на item, warehouse)

## 🔐 Переменные окружения

Локальная конфигурация в файле `.env` (пример в `.env.example`):

```
DB_URL=jdbc:postgresql://localhost:5432/warehouse_db
DB_USERNAME=warehouse_user
DB_PASSWORD=your_password
SPRING_PROFILES_ACTIVE=dev
```

## 📝 Лицензия

MIT License

## 👤 Автор

Clockер34

## 🎓 Образовательный проект

Данный проект создан в учебных целях для демонстрации:
- Spring Boot REST API разработки
- Работы с PostgreSQL и JPA
- Архитектурных паттернов
- Best practices в разработке Java приложений
